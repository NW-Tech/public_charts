{{- range $worker_name, $worker := .Values.workers.workers }}
{{- if $worker.enabled -}}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "celery-worker.fullname" $ }}-{{ $worker_name }}
  labels:
    {{- include "celery-worker.labels" $ | nindent 4 }}
spec:
  {{- if not $.Values.workers.autoscaling.enabled }}
  replicas: {{ $worker.replicaCount | default $.Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "celery-worker.selectorLabels" $ | nindent 6 }}
      celery-worker.io/worker: {{ $worker_name }}
  template:
    metadata:
      {{- with $.Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "celery-worker.selectorLabels" $ | nindent 8 }}
        celery-worker.io/worker: {{ $worker_name }}
    spec:
      {{- with $.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "celery-worker.serviceAccountName" $ }}
      securityContext:
        {{- toYaml $.Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "celery-worker.fullname" $ }}
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          image: "{{ $.Values.image.repository }}:{{ $.Values.image.tag | default $.Chart.AppVersion }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          command:
            {{- if $worker.entrypoint }}
            - {{ $worker.entrypoint }}
            {{- end }}
            - /bin/sh
            - -c
            - {{ include "celery-worker.command" (dict "worker" $worker "Values" $.Values) }}
          envFrom:  
            - configMapRef:
                name: {{ include "celery-worker.fullname" $ }}-configmap
            - secretRef:
                name: {{ include "celery-worker.fullname" $ }}-celery-urls
            {{- if $.Values.secrets.enabled }}
            {{- range $secretName, $_ := $.Values.secrets.data }}
            - secretRef:
                name: {{ include "celery-worker.fullname" $ }}-{{ $secretName }}
            {{- end }}
            {{- end }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          {{- range $probeType := (list "readiness" "liveness") }}
          {{ $probeType }}Probe:
            {{- include "celery-worker.probe" (dict "workerType" $worker.type "probeType" $probeType "Values" $.Values) | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml $worker.resources | nindent 12 }}
      {{- with $worker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- $pa := $worker.podAntiAffinity | default dict }}
      {{- if $pa.enabled }}
      affinity:
        podAntiAffinity:
          {{- if eq ($pa.podAntiAffinityType | default "preferred") "required" }}
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: {{ $pa.topologyKey | default "kubernetes.io/hostname" }}
              labelSelector:
                matchLabels:
                  {{- include "celery-worker.selectorLabels" $ | nindent 18 }}
                  celery-worker.io/worker: {{ $worker_name }}
          {{- else }}
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: {{ $pa.topologyKey | default "kubernetes.io/hostname" }}
                labelSelector:
                  matchLabels:
                    {{- include "celery-worker.selectorLabels" $ | nindent 20 }}
                    celery-worker.io/worker: {{ $worker_name }}
          {{- end }}
      {{- end }}
      {{- with $.Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

---

{{- end -}}
{{- end -}}
